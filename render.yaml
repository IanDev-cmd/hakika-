services:
  - type: web
    name: hakika
    runtime: node
    buildCommand: yarn
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_VERSION
        value: "24"
